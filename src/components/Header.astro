---
import HeaderLink from './HeaderLink.astro';
import { SITE_TITLE } from '../consts';
---

<header>
	<h2 class="site-title">
		<a href="/Blog/">
			<span class="quirk">R</span>OBIN'S   <span class="quirk">B</span>LOG
		</a>
	</h2>
	<!-- <div class="internal-links">
		<HeaderLink href="/Blog/">Home</HeaderLink>
		<HeaderLink href="/Blog/about">About</HeaderLink>
	</div> -->
</header>
<style>
	header {
		background-color: var(--base);
		border-bottom: 3px solid var(--subtle);
		border-top: 3px solid var(--subtle);

		grid-row: 1;
		grid-column: 1/3;
		
		padding: 0;
		margin-bottom: 1em;

		position: sticky;
		top: 2em;
	}

	.site-title {
		margin: 0;
		font-size: 4em;
		text-align: center;
		font-family: var(--font-display);
		color: var(--rose);
	}

	.quirk {
		font-family: var(--font-quirk);
		font-size: 1.5em;
	}

	h2 a,
	h2 a.active {
		text-decoration: none;
		color: var(--rose);
	}

	/* nav {
		display: flex;
		align-items: center;
		justify-content: center;
	} */
	
	/* nav a {
		padding: 1em 0.5em;
		color: var(--black);
		border-bottom: 4px solid transparent;
		text-decoration: none;
	}
	nav a.active {
		text-decoration: none;
	} */
	
	@media (max-width: 720px) {
		.social-links {
			display: none;
		}
	}
</style>
